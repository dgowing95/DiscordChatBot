# Top-level deps only (pip resolves the rest). torch is installed separately in
# the Dockerfile from PyTorch's CUDA index (cu128 wheels bundle the nvidia-*
# runtime libraries), so it is intentionally not pinned here.
fastapi==0.141.1
uvicorn[standard]==0.34.2
diffusers==0.33.1
# transformers must stay < 5: v5 flattened CLIPTextModel (no `.text_model`),
# and diffusers' from_single_file CLIP loader still dereferences it. Single-file
# checkpoints (the Juggernaut-XL model in prod) then fail at startup with
# "'CLIPTextModel' object has no attribute 'text_model'". Broke prod on
# v2.31-v2.37 (2026-09-16); CI cannot catch it because the image is only built,
# never run.
transformers==4.52.4
# Encodes prompts past CLIP's 77-token window (see _build_compel in main.py).
# Pinned to 2.1.1 on purpose: 2.4.0 requires transformers>=5 (see above), and
# every release from 2.2.0 on also pulls in `notebook` (the whole Jupyter server
# stack) for a library this service imports two names from.
compel==2.1.1
accelerate==1.7.0
huggingface-hub==0.32.3
safetensors==0.5.3
Pillow==11.2.1
numpy==2.2.6
