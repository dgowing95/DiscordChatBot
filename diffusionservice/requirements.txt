# Top-level deps only (pip resolves the rest). torch is installed separately in
# the Dockerfile from PyTorch's CUDA index (cu128 wheels bundle the nvidia-*
# runtime libraries), so it is intentionally not pinned here.
fastapi==0.141.1
uvicorn[standard]==0.52.4
# diffusers and transformers move together: transformers >= 5.6 flattened
# CLIPTextModel (no `.text_model`), and only diffusers >= 0.40 knows that in
# its single-file CLIP loader. diffusers 0.38 + transformers 5 crashed the
# Juggernaut-XL (single-file) pipeline at startup on v2.31-v2.37 (2026-09-16).
# CI cannot catch this because the image is only built, never run.
diffusers==0.40.0
transformers==5.16.1
# Encodes prompts past CLIP's 77-token window (see _build_compel in main.py).
# Compel 2.4 supports Transformers 5 and preserves chunked long prompts. It
# brings the Jupyter/notebook stack transitively; that trade-off is deliberate.
compel==2.4.0
accelerate==1.15.0
huggingface-hub==1.31.0
safetensors==0.8.0
Pillow==12.3.0
numpy==2.5.2
