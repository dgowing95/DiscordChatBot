# Top-level deps only (pip resolves the rest). torch is installed separately in
# the Dockerfile from PyTorch's CUDA index (cu128 wheels bundle the nvidia-*
# runtime libraries), so it is intentionally not pinned here.
fastapi==0.115.12
uvicorn[standard]==0.34.2
diffusers==0.38.0
transformers==5.10.4
# Encodes prompts past CLIP's 77-token window (see _build_compel in main.py).
# Compel 2.4 supports Transformers 5 and preserves chunked long prompts. It
# brings the Jupyter/notebook stack transitively; that trade-off is deliberate.
compel==2.4.0
accelerate==1.15.0
huggingface-hub==1.31.0
safetensors==0.8.0
Pillow==12.3.0
numpy==2.5.3
